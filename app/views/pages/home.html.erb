<% unless user_signed_in? %>

	<%= render 'pages/basic_landing_page' %>

<%else%>
	
	<% if current_user.is_tasker %>

		<%= render 'users/tasker_dashboard' %>

	<%else%>

		<%= render 'users/non_tasker_dashboard' %>

	<%end%>
	
<%end%>

